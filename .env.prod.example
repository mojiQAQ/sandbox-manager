# Sandbox Manager 生产环境变量
# 复制为 .env 后修改

# 对外暴露的端口（Nginx Proxy Manager 转发目标）
SBXMGR_PORT=8000

# 外部访问地址（公网 IP 或域名，用于 URL 类型沙盒的访问链接）
SBXMGR_EXTERNAL_HOST=113.31.119.251

# 调试模式（生产环境请设为 false）
SBXMGR_DEBUG=false
